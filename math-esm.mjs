const add = (a, b) =>{
    return a + b;
};

// export default add;

const  subtract = (a, b) => {
    return a - b
}

// this is third pattend as whole object is imported there
export default {
    add,
    subtract,
};

// 2nd pattend

// export default (a, b) => {
//     return a + b;
// };